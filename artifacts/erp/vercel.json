{
  "buildCommand": "cd ../.. && pnpm install --no-frozen-lockfile && cd artifacts/erp && rm -rf dist && pnpm run build",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://bizcor-erp-api-server.vercel.app/api/:path*" },
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
