{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && cd artifacts/erp && pnpm run build",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
